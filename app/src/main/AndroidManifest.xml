<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a8477_daniellaqueroz_androidprototype">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.8477_DaniellaQueroz_AndroidPrototype.NoActionBar">
        <activity
            android:name=".actvities.addRecipeScrollingActivity"
            android:label="@string/title_activity_add_recipe_scrolling"
            android:theme="@style/Theme.8477_DaniellaQueroz_AndroidPrototype.NoActionBar.NoActionBar" />
        <activity android:name=".Signin" />
        <activity
            android:name=".actvities.recipeDetailScrollingActivity"
            android:label="@string/title_activity_monster_detail_scrolling"
            android:theme="@style/Theme.8477_DaniellaQueroz_AndroidPrototype.NoActionBar" />
        <activity android:name=".MainActivity" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>